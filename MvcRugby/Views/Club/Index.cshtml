@model IEnumerable<MvcRugby.Models.Club>

@{
    ViewData["Title"] = "Index";
}
<h1>Club</h1>
<br>

<div class="container">
  <hr />
    <div class="p-3 mb-2 bg-dark text-white">
      <a asp-action="Create" class="btn btn-primary">Add New Club</a>
    </div>
  <hr />
</div>
<br>

<!--@if (Model != null && Model.Any())-->
@* { *@
<table class="table">
    <thead>
        <tr>
            <th scope="col">Id </th>
            <th scope="col">SportRadar Competitor Id</th>
            <th scope="col">Club Name</th>
            <th scope="col">Qualifier</th>
        </tr>
    </thead>
    <table class="table table-striped table-hover">
        <tbody>
            @foreach (var club in Model)
            {
            <tr>
                <td>@club.Id</td>
                <td>@club.SportRadar_Competitor_Id</td>
                <td>@club.Club_Name</td>
                <td>@club.Qualifier</td>
                <td>
                    <a class="btn btn-primary" asp-controller="Club" asp-action="Create" asp-route-id="@club.Id">Select Club</a>
                    <a class="btn btn-warning" asp-controller="Club" asp-action="Edit" asp-route-id="@club.Id">Edit</a>
                    <a class="btn btn-danger" asp-controller="Club" asp-action="Delete" asp-route-id="@club.Id">Delete</a>
                </td>
            </tr>
            }
        </tbody>
    </table>
</table>
@* }
else
{
  <p>No Clubs found</p>
} *@

<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Id</th>
            <th scope="col">SportRadar Competitor Id</th>
            <th scope="col">Club Name</th>
            <th scope="col">Qualifier</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <table class="table table-striped table-hover">
                <tbody>
                    <tr>
                        <th scope="row">1</th>
                        <td>@club.Id</td>
                        <td>Otto</td>
                        <td></td>
                    </tr>
                    <tr>
                        <th scope="row">2</th>
                        <td>Jacob</td>
                        <td>Thornton</td>
                        <td></td>
                    </tr>
                        <tr>
                        <th scope="row">3</th>
                        <td colspan="2">Larry the Bird</td>
                        <td></td>
                    </tr>
                </tbody>
            </table>
        </tr>
    </tbody>
</table>   
 
<br>
<br>